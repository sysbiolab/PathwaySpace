% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation-pspace.R
\name{annotation_pspace_signal}
\alias{annotation_pspace_signal}
\title{Annotation Functions for PathwaySpace Plots}
\usage{
annotation_pspace_signal(
  ps,
  title = activeFeature(ps),
  colors = pspace.cols(),
  bg.color = "grey95",
  si.color = "grey85",
  si.alpha = 1,
  zlab = "Density",
  zlim = NULL,
  slices = 25,
  interpolate = FALSE,
  ...
)
}
\arguments{
\item{ps}{A \linkS4class{PathwaySpace} object containing a valid signal
projection. Run \code{\link{circularProjection}} or
\code{\link{polarProjection}} before calling this function.}

\item{title}{A string used as the plot title. Defaults to the active
feature name returned by \code{\link{activeFeature}}.}

\item{colors}{A character vector of colors used to build the signal
palette. Defaults to \code{\link{pspace.cols}}.}

\item{bg.color}{A string specifying the background color, used for
zero-signal or masked regions. Defaults to \code{"grey95"}.}

\item{si.color}{A single color for silhouette.
(see \code{\link{silhouetteMapping}}).}

\item{si.alpha}{A transparency level in \verb{[0, 1]}, used to adjust the
opacity of the silhouette. This parameter is useful for improving the
perception of a background image, when one is available.}

\item{zlab}{The title for the 'z' axis of the image signal.}

\item{zlim}{The 'z' limits of the plot (a numeric vector with two numbers).
If NULL, limits are determined from the range of the input values.}

\item{slices}{An integer specifying the number of discrete color levels
used to quantize the signal. For \code{"negpos"} scale types, this is
rounded up to the nearest even number.}

\item{interpolate}{A logical value indicating whether to apply linear
interpolation with \code{\link[ggplot2]{geom_raster}}.}

\item{...}{Additional parameters passed to the title annotation;
see \code{\link[ggplot2]{geom_text}}.}
}
\value{
A list of \code{ggplot2} layer objects that can be added to a
\code{ggplot()} call with \code{+}.
}
\description{
\code{annotation_pspace_signal()} annotates a \code{ggplot}-based
\code{PathwaySpace} plot with the projected signal layer, rendered as a
raster heatmap bounded to the normalized unit space \code{[0, 1]}.
}
\examples{
data("gtoy1", package = "RGraphSpace")
ps <- buildPathwaySpace(gtoy1, nrc = 100)
vertexSignal(ps) <- 1
ps <- circularProjection(ps)

\dontrun{
ggplot(ps) +
  annotation_pspace_signal(ps, si.alpha = 0.8) +
  theme_gspace_coords(is_norm = TRUE)
}

}
\seealso{
\code{\link{circularProjection}}
}
